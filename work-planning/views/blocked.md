# Blocked Work

No task is blocked.

## Snapshot Risks

- `AV2-CONTRACT` targets a dirty repo and requires a clean execution base.
- `AV2-BACKEND` targets a dirty repo and requires a clean execution base.
- `AV2-FRONTEND` targets a dirty repo and requires a clean execution base.
- `AV2-BENCHMARK` targets a dirty repo and requires a clean execution base.
- `AV2-SCHEMA-MIGRATION` targets a dirty repo and requires a clean execution base.
- `AV2-IMAGE-FILE-ID` targets a dirty repo and requires a clean execution base.
- `AV2-RELEASE` targets a dirty repo and requires a clean execution base.
